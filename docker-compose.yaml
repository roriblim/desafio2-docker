services:

  node:
    build:
      context: ./node
    container_name: node-server
    ports: 
      - "3000:3000"

  nginx:
    build:
      context: ./nginx
    container_name: nginx-proxy
    tty: true
    ports: 
    - "8080:80"
